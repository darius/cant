(make-trait ejector-primitive me

  (to (~ .eject value)
    (__eject me value))

  (to ~.enabled?
    (ejector-enabled? me))

  (to (~ .selfie sink)
    (sink .display (if (ejector-enabled? me)
                       "#<ejector>"
                       "#<ejector disabled>")))

  )
