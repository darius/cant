;; An ordinary Scheme procedure like __+ and so on.

;; There's a magical behavior internal to the player, not expressed in
;; this script: if the message is a tuple, then handle it with the
;; Scheme code of this procedure-primitive. For all other message
;; types, use the script below (which in practice means to use
;; miranda-trait).

(make-trait procedure-primitive me
  )
