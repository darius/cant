;; 1000-digit Fibonacci number

;; copied from ./02.cant
(let fibs (begin ((a 1) (b 1))
            (link/lazy a (: (loop b (+ a b))))))

(let (~ i fib-i) (~.first (for drop-while (((~ index value) fibs.items))
                            (< (value .log 10) 999)))) ;; 999 digits after the leftmost
(out .print i.+) ;; Increment because their index convention is 1-based
