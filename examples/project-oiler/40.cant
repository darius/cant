;; Champernowne's constant

(to (answer)
  (to (champ index)
    (hey (champ-digits index.-)
         (:: (out .say "~w: ~w\n" index it))))
  (* @(each (:: (champ (10 .** it))) (0 .thru 6))))

;; Super dumb. TODO smarter
(let champ-digits (gather/lazy ~.digits (iterate ~.+ 1)))

(out .print (answer)) ;; 210
