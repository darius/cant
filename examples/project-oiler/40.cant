;; Champernowne's constant

(to (answer)
  (to (champ index)
    (out .say "~w: ~w\n" index (champ-digits index.-))
    (champ-digits index.-))
  (* @(each (:: (champ (10 .** it))) (0 .thru 6))))

(let champ-digits
  ;; Super dumb. TODO smarter
  (begin champing ((i 1))
    (begin copying ((ds (reverse i.digits)))
      (hm (if ds.none? (champing i.+))
          (else (link/lazier ds.first (: (copying ds.rest))))))))

(out .print (answer)) ;; 210
