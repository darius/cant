(import (use "eg/lambda/terp")
  run)

(let test-names '("trivial1.cant"
                  "trivial2.cant"
                  "church.cant"
                  "factorial.cant"
                  "reverse.cant"
                  ))

(for each! ((test test-names))
  (format "\nTesting ~d\n" test)
  (print (run (chain "eg/lambda/eg/" test))))
